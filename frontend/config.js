const config = {
    API_URL: window.location.hostname === 'localhost' 
        ? 'http://localhost:8000' 
        : 'https://blog-app-backend-xxxx.onrender.com' // We'll update this after backend deployment
};
